h1 Edit User Expense

= simple_form_for @user_expense do |f|
  = f.error_notification
  p
    | Amount remaining to adjust: #{@user_expense.adjust_amount}
  = f.input :amount

  = f.button :submit, 'Update', class: 'btn btn-primary'
  = link_to 'Back', expense_path(@user_expense.expense)